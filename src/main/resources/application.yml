server:
  port: 8080
  error:
    include-message: always

spring:
  datasource:
    url: jdbc:mysql://root:1234@localhost:3306/heladeria?createDatabaseIfNotExist=true
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false